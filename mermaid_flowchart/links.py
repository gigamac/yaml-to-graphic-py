class LinksDumb:
    from_to = '{} --> {}'
    thick = '{} ==> {}'
    non_directional = '{}---{}'
    optional_from_to = '{}-.->{}'
    optional_non_directional = '{}-.-{}'


class LinksTexted:
    from_to = '{} -- {} -->{}'
    non_directional = '{} --- {} ---{}'
    optional_non_directional = '{} -. {} .-{}'
    optional_from_to = '{} -. {} .->{}'
    thick = '{} == {} ==>{}'
    thick_optional = '{} =. {} .=>{}'
